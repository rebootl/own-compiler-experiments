

var(a, 10)
;var(a, 10)

while(gt(a, 1), {

  dec(a)

  var(b, 8)
  var(c, int_to_str(123))
  ;var(c, int_to_str(123))

  if(lt(a, 5), {
    break()
  })

  print_i(a)
})

; -> this is useful to test if the freeing of the stack works
var(b, 10)
print_i(b)
